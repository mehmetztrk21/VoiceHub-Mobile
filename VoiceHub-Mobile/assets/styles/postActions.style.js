import { StyleSheet } from "react-native";

export default StyleSheet.create({
  postActions: {
    flex: 1,
    flexDirection: "row",
    justifyContent:"space-around",
  },
  pactions: {
    marginTop: 10,
    marginBottom: 10,
    flexDirection:"row",
    alignItems:"center"
  }
});