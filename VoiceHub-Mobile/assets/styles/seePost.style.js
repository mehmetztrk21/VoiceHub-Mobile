import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
    },

    postView: {
        paddingTop: 5,
        flexDirection: "column",
        width: "100%",
      },
      scroll: {
        marginBottom: 60,
        width: "100%",
      },
});