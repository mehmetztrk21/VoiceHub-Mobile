import { StyleSheet } from "react-native";

export default StyleSheet.create({
  postActions: {
    flexDirection: "row",
    justifyContent: "space-around",
  },
  pactions: {
    marginTop: 10,
    marginBottom: 10,
    flexDirection: "row",
    alignItems: "center"
  }
});